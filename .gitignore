__pycache__/
*.py[cod]
.DS_Store
.env
results.json
data/catalog.jsonl
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log

# --- ByteMe additions ---
# Never commit the catalog in any form (CLAUDE.md section 8).
data/catalog.jsonl.gz
# Experiment outputs are per-person and would collide constantly. day0.json is
# the exception: it is the shared reference baseline that src/eval/run.py diffs
# against, so it is tracked deliberately.
runs/*
!runs/day0.json
